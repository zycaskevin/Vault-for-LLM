CAT:error | T:WSL2 Chrome CDP 三層橋接方案 | S:# WSL2 Chrome CDP 三層橋接方案 | F1:Chrome — `--remote-debugging-port=9222` 監聽 `127.0… | F2:Chrome 147+ `--remote-debugging-port` 必須先 kill 所有… | F3:必須用獨立 profile (`--user-data-dir=cdp-chrome-profil… | F4:Node.js ESM `import('ws')` 無法解析全局安裝的模組 — 放棄 Node，… | F5:`/json/new` Chrome 要求 PUT verb，不是 GET（回傳 "Using u… | A1:必須先 kill 所有 chrome. | A2:必須用獨立 profile (`--user-data-dir=cdp-chr… | A3:要求 PUT verb，不是 GET（回傳 "Using unsafe HTT… | SRC:20260416-wsl2-chrome-cdp-bridge.md