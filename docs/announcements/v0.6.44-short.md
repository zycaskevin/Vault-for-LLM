# Vault-for-LLM v0.6.44

This is a small setup-agent template fix release.

## Fixed

- Supabase LaunchAgent templates now write logs to `supabase-sync.log` and `supabase-sync.err.log`.
- Added regression coverage so Supabase scheduled sync templates do not reuse Obsidian log filenames.

No database or schema behavior changed in this release.
